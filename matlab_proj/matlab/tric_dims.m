function [n, J] = tric_dims()
%TRIC_DIMS Размерности модели. Владелец: A.
%
%   n — число ячеек (осевых срезов машины)
%   J — число классов размеров дисперсной фазы
%
% Литералы, а не поля шины: codegen должен вывести фиксированные
% размеры массивов на этапе компиляции. Если поставить n в
% Simulink.Parameter, он станет тунабельным и zeros(n,1) не свернётся.
%
% Если MATLAB Function block всё же ругается на переменный размер —
% замените вызовы [n,J] = tric_dims() на литералы прямо в коде.
%#codegen

n = 25;
J = 40;
end
